# Lab 1 — REST API + MongoDB + Frontend

A dishes API using **Node.js / Express / MongoDB (Mongoose)** with a minimal HTML/JS frontend.

## How to run locally

1. Install dependencies
   ```bash
   npm install
